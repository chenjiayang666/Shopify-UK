{%- unless section.settings.full_width -%}
  {%- render 'section-spacing-collapsing' -%}
{%- endunless -%}

<style>
  #shopify-section-{{ section.id }} {
    {%- if section.settings.full_width -%}
      --section-outer-spacing-block: 0;
    {%- endif -%}

    --content-over-media-overlay: {{ section.settings.overlay_color.rgb }} / {{ section.settings.overlay_opacity | divided_by: 100.0 }};

    {%- if section.settings.allow_transparent_header -%}
      margin-block-start: calc(-1 * var(--header-height) * var(--section-is-first));
    {%- endif -%}
  }

  {%- unless section.settings.full_width -%}
    #shopify-section-{{ section.id }}:first-child .section[allow-transparent-header] {
      padding-block-start: max(var(--section-inner-max-spacing-block), var(--header-height));
    }
  {%- endunless -%}

  #shopify-section-{{ section.id }} .video-wrapper{
      width: 100%;
      max-width: max-content;
      grid-area: 1 / 1 / span 3 / span 3;
  }
  #shopify-section-{{ section.id }} .video-control {
        position: absolute;
    bottom: 0;
    right: unset;
    left: 0;
    transform: translate(0%, 0%) scale(0.6);
  }
    #shopify-section-{{ section.id }} .section-maxwidth{
        max-width:{{ section.settings.max-width }}px;
            margin: 0 auto;
    }
  @media(max-width:750px){
      #shopify-section-{{ section.id }} .video-control {
    transform: translate(0%, 0%) scale(0.4);
  }
  }
</style>

<div
  id="{{ section.settings.anchor_id }}" {% render 'section-properties' %} {% if section.settings.allow_transparent_header %}allow-transparent-header{% endif %}>
  {%- capture class -%}content-over-media   {% if section.settings.video_size != 'auto' %}content-over-media--{{ section.settings.video_size }}{% endif %} {% if section.settings.full_width %}full-bleed{% else %}section-maxwidth shadow-block rounded-lg{% endif %}{%- endcapture -%}

  <div {% render 'surface', class: class, text_color: section.settings.text_color %}>
    {%- capture poster -%}
      {%- assign poster_image = section.settings.poster | default: section.settings.video.preview_image -%}

      {%- if section.settings.autoplay == false and poster_image != blank -%}
        {%- capture sizes -%}{% if section.settings.full_width %}100vw{% else %}(max-width: 740px) calc(100vw - 40px), (max-width: 999px) calc(100vw - 64px), min({{ settings.page_width }}px, 100vw - 96px){% endif %}{%- endcapture -%}
        {{- poster_image | image_url: width: poster_image.width | image_tag: sizes: sizes, widths: '200,300,400,500,600,700,800,900,1000,1200,1400,1600,1800,2000,2200,2400,2600,2800,3000,3200' -}}
      {%- endif -%}
    {%- endcapture -%}

    {%- if section.settings.video -%}
      {%- if section.settings.autoplay -%}
        {%- assign show_controls = false -%}
      {%- else -%}
        {%- assign show_controls = true -%}
      {%- endif -%}

      {%- if section.settings.mobile_video != blank -%}
        {%- assign poster_image = section.settings.poster_mob | default: section.settings.mobile_video.preview_image -%}
        {%if section.settings.show_step and show_controls == false%}
          <div class="video-wrapper sm:hidden">
        {% endif %}
          <video-media {% if section.settings.autoplay %}autoplay{% endif %} class="sm:hidden" {% if section.settings.video_size == 'auto' %}style="--aspect-ratio: {{ section.settings.mobile_video.aspect_ratio }}"{% endif %}>
            {{- section.settings.mobile_video | video_tag: class: 'object-cover sm:hidden', controls: show_controls, playsinline: true, muted: section.settings.autoplay, loop: section.settings.autoplay, preload: 'metadata', image_size: '400x' -}}
            {{- poster_image | image_url: width: poster_image.width | image_tag: sizes: '100vw', widths: '200,300,400,500,600,700,800,900,1000,1200' -}}
          </video-media>
        {%if section.settings.show_step and show_controls == false %}
          <div class="video-control">
            <svg width="70" height="70">
              <circle class="progress-bg" cx="35" cy="35" r="30"></circle>
              <circle class="progress-bar" cx="35" cy="35" r="30"></circle>
            </svg>
            <div class="icon play"></div>
          </div>
        </div>
      {% endif %}
      {%- endif -%}

      {%if section.settings.show_step and show_controls == false%}
        <div class="video-wrapper  {% if section.settings.mobile_video != blank %}hidden sm:block{% endif %} ">
      {% endif %}

        <video-media {% if section.settings.autoplay %}autoplay{% endif %} {% if section.settings.mobile_video != blank %}class="hidden sm:block"{% endif %} {% if section.settings.video_size == 'auto' %}style="--aspect-ratio: {{ section.settings.video.aspect_ratio }}"{% endif %}>
          {{- section.settings.video | video_tag: playsinline: true, preload: 'metadata', muted: section.settings.autoplay, loop: section.settings.autoplay, controls: show_controls, image_size: '800x' -}}
          {{- poster -}}
        </video-media>
      {%if section.settings.show_step and show_controls == false %}

        <div class="video-control">
          <svg width="70" height="70">
            <circle class="progress-bg" cx="35" cy="35" r="30"></circle>
            <circle class="progress-bar" cx="35" cy="35" r="30"></circle>
          </svg>
          <div class="icon play"></div>
        </div>
      </div>
      {% endif %}


    {%- else -%}
      <video-media host="{{ section.settings.external_video_url.type }}" {% if section.settings.autoplay %}autoplay class="pointer-events-none"{% endif %}>
        {{- poster -}}

        <template>
          {%- if section.settings.external_video_url.type == 'youtube' -%}
            <iframe src="https://www.youtube.com/embed/{{ section.settings.external_video_url.id }}?playsinline=1&{% if section.settings.autoplay %}autoplay=1&controls=0&mute=1&loop=1&{% endif %}playlist={{ section.settings.external_video_url.id }}&enablejsapi=1&rel=0&modestbranding=1&origin={{ 'https://' | append: request.host | url_encode }}" allow="autoplay; encrypted-media" allowfullscreen="allowfullscreen"></iframe>
          {%- elsif section.settings.external_video_url.type == 'vimeo' -%}
            <iframe src="https://player.vimeo.com/video/{{ section.settings.external_video_url.id }}?autopause=1&{% if section.settings.autoplay %}background=1&autoplay=1&loop=1&muted=1&{% endif %}transparent=0&responsive=1&portrait=0&title=0&byline=0&color={{ settings.text_color | remove_first: '#' }}" allow="autoplay; encrypted-media;" allowfullscreen="allowfullscreen"></iframe>
          {%- endif -%}
        </template>
      </video-media>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      <div class="place-self-center text-center">
        <div class="prose">
          {%- for block in section.blocks -%}
            {%- case block.type -%}
              {%- when 'play' -%}
                {%- unless section.settings.autoplay -%}
                  <button class="video-play-button" type="button" {{ block.shopify_attributes }}>
                    <span class="sr-only">{{ 'general.accessibility.play_video' | t }}</span>
                    {%- render 'icon' with 'play-button', width: 48, height: 48 -%}
                  </button>
                {%- endunless -%}

              {%- when 'subheading' -%}
                {%- if block.settings.text != blank -%}
                  <{{ block.settings.subheading_tag }} class="{{ block.settings.subheading_size }}" {{ block.shopify_attributes }}>{{ block.settings.text | escape }}</{{ block.settings.subheading_tag }}>
                {%- endif -%}

              {%- when 'heading' -%}
                {%- if block.settings.text != blank -%}
                  <p class="{{ block.settings.heading_tag }}" {{ block.shopify_attributes }}>{{ block.settings.text | escape }}</p>
                {%- elsif block.settings.title_seo != blank -%}
                  <{{ block.settings.heading_tag }} class="{{ block.settings.heading_size }}" {{ block.shopify_attributes }}>
                    {{ block.settings.title_seo }}
                  </{{ block.settings.heading_tag }}>
                {%- endif -%}

              {%- when 'richtext' -%}
                {%- if block.settings.content != blank -%}
                  <div {{ block.shopify_attributes }}>
                    {{- block.settings.content -}}
                  </div>
                {%- endif -%}

              {%- when 'button' -%}
                {%- if block.settings.text != blank -%}
                  {% render 'button', content: block.settings.text, href: block.settings.url, size: block.settings.size, style: block.settings.style, background: block.settings.background, text_color: block.settings.text_color, block: block %}
                {%- endif -%}
            {%- endcase -%}
          {%- endfor -%}
        </div>
      </div>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Video",
  "class": "shopify-section--video",
  "tag": "section",
  "disabled_on": {
    "groups": ["header", "custom.overlay"]
  },
  "settings": [
        {
      "type": "text",
      "id": "anchor_id",
      "label": "Anchor ID",
      "info": "Unique ID for anchor links",
      "default": "minimum-slide"
    },
    {
      "type": "paragraph",
      "content": "For best visual results and avoid platform branding, use a MP4 file."
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full width",
      "default": true
    },
       {
      "type": "range",
      "id": "max-width",
      "max": 1920,
      "min": 900,
      "step": 20,
      "default": 1200,
      "label": "Section width"
    },
    {
      "type": "checkbox",
      "id": "allow_transparent_header",
      "label": "Allow transparent header",
      "info": "This setting only applies when this section is the first one.",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Autoplay",
      "info": "Video are muted to allow autoplay. For best results, do not add any content on top of the video when autoplay is enabled.",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_step",
      "label": "progress bar",
      "info": "Display progress bar",
      "default": false
    },
    
    {
      "type": "select",
      "id": "video_size",
      "label": "Video size",
      "info": "Choose \"Original video ratio\" to avoid video cropping.",
      "options": [
        {
          "value": "auto",
          "label": "Original aspect ratio"
        },
        {
          "value": "sm",
          "label": "Small"
        },
        {
          "value": "md",
          "label": "Medium"
        },
        {
          "value": "lg",
          "label": "Large"
        },
        {
          "value": "fill",
          "label": "Fill screen"
        }
      ],
      "default": "auto"
    },
    {
      "type": "video",
      "id": "video",
      "label": "Video",
      "info": "Replaces external video if both are set."
    },
    {
      "type": "video",
      "id": "mobile_video",
      "label": "Mobile video",
      "info": "Default to main video if none is set."
    },
    {
      "type": "video_url",
      "id": "external_video_url",
      "accept": ["vimeo", "youtube"],
      "label": "Video URL",
      "default": "https://www.youtube.com/watch?v=_9VUPq3SxOc"
    },
    {
      "type": "image_picker",
      "id": "poster",
      "label": "Poster image",
      "info": "3200 x 1600px .jpg recommended. Poster is not visible when autoplay is enabled."
    },
        {
      "type": "image_picker",
      "id": "poster_mob",
      "label": "Poster image mobile",
      "info": "3200 x 1600px .jpg recommended. Poster is not visible when autoplay is enabled."
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "label": "Overlay opacity",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 30
    },
   
  ],
  "blocks": [
    {
      "type": "play",
      "name": "Play button",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "Play button is hidden when autoplay is enabled."
        }
      ]
    },
    {
      "type": "subheading",
      "name": "Subheading",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Subheading"
        },
        {
          "type": "select",
          "id": "subheading_tag",
          "label": "Subheading HTML tag",
          "options": [
            {
              "value": "h1",
              "label": "H1"
            },
            {
              "value": "h2",
              "label": "H2"
            },
            {
              "value": "h3",
              "label": "H3"
            },
            {
              "value": "h4",
              "label": "H4"
            },
            {
              "value": "h5",
              "label": "H5"
            },
            {
              "value": "h6",
              "label": "H6"
            },
            {
              "value": "p",
              "label": "Paragraph (p)"
            },
            {
              "value": "div",
              "label": "Div(不具有语义)"
            }
          ],
          "default": "p"
        },
        {
          "type": "select",
          "id": "subheading_size",
          "label": "Subheading size",
          "options": [
            {
              "value": "dev-xx-large",
              "label": "XX-Large"
            },
            {
              "value": "dev-x-large",
              "label": "X-Large"
            },
            {
              "value": "dev-large",
              "label": "Large"
            },
            {
              "value": "dev-medium",
              "label": "Medium"
            },
            {
              "value": "dev-small",
              "label": "Small"
            },
            {
              "value": "dev-x-small",
              "label": "X-Small"
            },
            {
              "value": "dev-xx-small",
              "label": "XX-Small"
            }
          ],
          "default": "dev-xx-large"
        }
      ]
    },
    {
      "type": "heading",
      "name": "Heading",
      "settings": [
        {
          "type": "header",
          "content": "原有用法"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Heading",
          "info": "如果使用Seo Heading，则清空该输入框。因为代码逻辑是：该输入框有值则使用该值(就不会使用SEO用法了)。"
        },
        {
          "type": "header",
          "content": "SEO用法"
        },
        {
          "type": "textarea",
          "id": "title_seo",
          "label": "SEO Text"
        },
        {
          "type": "select",
          "id": "heading_tag",
          "label": "SEO Text HTML tag",
          "options": [
            {
              "value": "h1",
              "label": "H1"
            },
            {
              "value": "h2",
              "label": "H2"
            },
            {
              "value": "h3",
              "label": "H3"
            },
            {
              "value": "h4",
              "label": "H4"
            },
            {
              "value": "h5",
              "label": "H5"
            },
            {
              "value": "h6",
              "label": "H6"
            },
            {
              "value": "p",
              "label": "Paragraph (p)"
            },
            {
              "value": "div",
              "label": "Div(不具有语义)"
            }
          ],
          "default": "p"
        },
        {
          "type": "header",
          "content": "其他设置"
        },
        {
          "type": "select",
          "id": "heading_size",
          "label": "Heading size",
          "options": [
            {
              "value": "dev-xx-large",
              "label": "XX-Large"
            },
            {
              "value": "dev-x-large",
              "label": "X-Large"
            },
            {
              "value": "dev-large",
              "label": "Large"
            },
            {
              "value": "dev-medium",
              "label": "Medium"
            },
            {
              "value": "dev-small",
              "label": "Small"
            },
            {
              "value": "dev-x-small",
              "label": "X-Small"
            },
            {
              "value": "dev-xx-small",
              "label": "XX-Small"
            }
          ],
          "default": "dev-xx-large",
          "info": "这个选框的值，Text和SEO Text都会应用上。"
        }
      ]
    },
    {
      "name": "Paragraph",
      "type": "richtext",
      "settings": [
        {
          "type": "richtext",
          "id": "content",
          "label": "Content",
          "default": "<p>Use video to showcase product features or to create a unique atmosphere on your store.</p>"
        }
      ]
    },
    {
      "type": "button",
      "name": "Button",
      "settings": [
        {
          "type": "select",
          "id": "style",
          "label": "Style",
          "options": [
            {
              "value": "outline",
              "label": "Outline"
            },
            {
              "value": "fill",
              "label": "Fill"
            }
          ],
          "default": "outline"
        },
        {
          "type": "select",
          "id": "size",
          "label": "Size",
          "options": [
            {
              "value": "sm",
              "label": "Small"
            },
            {
              "value": "base",
              "label": "Medium"
            },
            {
              "value": "lg",
              "label": "Large"
            },
            {
              "value": "xl",
              "label": "X-Large"
            }
          ],
          "default": "lg"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Button text"
        },
        {
          "type": "url",
          "id": "url",
          "label": "Link"
        },
        {
          "type": "color",
          "id": "background",
          "label": "Background"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text",
          "default": "#ffffff"
        }
      ]
    },
    {
      "type":"video_schema",
      "name":"video_schema",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Video Title"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Video Description"
        },
        {
          "type": "text",
          "id": "thumbnailurl",
          "label": "Video Thumbnailurl"
        },
        {
          "type": "text",
          "id": "uploadDate",
          "label": "Video UploadDate"
        },
        {
          "type": "text",
          "id": "contenturl",
          "label": "Video Contenturl"
        },
        {
          "type": "text",
          "id": "embedUrl",
          "label": "Video EmbedUrl"
        },
      ]
    }
  ],
  "presets": [
    {
      "name": "Video",
      "blocks": [
        {
          "type": "play"
        },
        {
          "type": "heading",
          "settings": {
            "text": "Video"
          }
        }
      ]
    }
  ]
}
{% endschema %}